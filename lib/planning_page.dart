import 'package:flutter/material.dart';

class PlanningPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Financial Planning'),
      ),
      body: Center(
        child: Text('Financial planning interface will be here'),
      ),
    );
  }
}
