import 'package:flutter/material.dart';

class ExpensesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Track Expenses'),
      ),
      body: Center(
        child: Text('Expense tracking interface will be here'),
      ),
    );
  }
}
